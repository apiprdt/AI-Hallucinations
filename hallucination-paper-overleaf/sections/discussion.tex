\section{Discussion}
\label{sec:discussion}

We organize this discussion by explicitly separating empirical observations from interpretive speculation, identifying alternative explanations, and establishing rigorous boundaries for our claims.

\subsection{Distributional Bias in Hallucination Generation}
\paragraph{Observation.} Across both datasets, naturally generated hallucinations (C3) are overwhelmingly classified as Contextual Confabulation (CC, $\sim$80\%), with Structural Phantom (SP) accounting for only 1.3--2.4\% of outputs. Despite this rarity, forced SP hallucinations (C4a) were associated with the largest observed ROC-AUC increase on BBBP, though we emphasize this is based on forced perturbations rather than naturally occurring frequencies.

\paragraph{Interpretation.} We characterize this distributional asymmetry as a generative bias toward generic, scientifically plausible narratives over structurally specific confabulations. This labeling is purely descriptive and does not imply a causal generative mechanism within the LLM architecture.

\paragraph{Alternative Explanations.} The low SP rate may reflect prompt design interactions rather than an intrinsic model bias; different prompt formulations or higher temperatures might elicit more structurally specific outputs. Additionally, our rule-based classifier may conservatively undercount SP instances if the generated text employs structural synonyms not captured by our heuristic keyword dictionary.

\subsection{Task-Dependent Asymmetry}
\paragraph{Observation.} On BBBP, hallucination augmentation (C3, C4a--c) was associated with directionally improved ROC-AUC relative to the SMILES-only baseline. The LLM baseline (C0, SMILES only) yielded a ROC-AUC of 0.533 [0.471, 0.593]. Factual augmentation (C1) produced a marginal, non-significant increase to 0.541. Chemical priming (C2) yielded 0.613, while pure non-chemical gibberish (C2b) yielded 0.553. Notably, the non-chemical gibberish condition (C2b) outperformed the purely factual condition (C1). Conversely, on BACE, all hallucination conditions degraded performance, with C3 reducing ROC-AUC from 0.674 to 0.504. The BACE prediction distribution exhibited pronounced compression, shifting from a bimodal pattern to a narrow unimodal distribution centered near $p \approx 0.17$.

\paragraph{Interpretation.} This task-dependent asymmetry suggests that hallucination utility is highly sensitive to the target domain. One plausible interpretation is that the LLM's pre-training corpus contains more robust latent representations of general physicochemical properties (relevant to BBBP) than specific enzymatic mechanisms (BACE), leading to constructive textual perturbation in the former and destructive interference in the latter.

\paragraph{Alternative Explanations.} We emphasize that these observed gains on BBBP do not demonstrate that hallucinations enhance genuine molecular reasoning. The improvements may partially reflect latent statistical correlations introduced by textual perturbation that happen to align with task-relevant physicochemical descriptors in the BBBP evaluation set. Furthermore, the observation that non-chemical gibberish (C2b, 0.553) outperformed factual text (C1, 0.541) strongly suggests that the LLM may be responding to prompt length, formatting, or linguistic complexity, rather than extracting utility solely from relevant semantic content. While textual perturbations induce directional shifts in raw prediction scores, none of the observed ROC-AUC improvements on the BBBP dataset reached statistical significance ($p > 0.05$). The BACE degradation, by contrast, was statistically significant across most conditions. This BACE degradation might be partially confounded in the Mechanism Fabrication (C4c) condition: because BACE-1 is the implied target of the BACE dataset, the MF text may inadvertently contain confounding information about BACE-1 itself, creating semantic interference rather than a general structural perturbation. We cannot distinguish among these interacting mechanisms with the current experimental design.

\subsection{Semantic Content vs.\ Stylistic Priming}
\paragraph{Observation.} The random-permutation control (C5) preserved scientific register while disrupting molecule-description semantic alignment. C5 produced no discriminative improvement over baseline (ROC-AUC 0.481 vs.\ 0.533 for C0 on BBBP), despite utilizing text that is linguistically indistinguishable from C3 hallucinations.

\paragraph{Interpretation.} This dissociation is consistent with the hypothesis that the performance shifts observed under C3 are not attributable solely to generic scientific writing style or prompt length, but depend on some degree of semantic interaction between the description and the target molecule.

\paragraph{Boundary.} The C5 control uses a random permutation ($seed=42$). While this approach minimizes the risk of structural proximity bias common in simple circular shifts, future work should employ even more rigorous semantic isolation, such as adversarial embedding-based negative sampling or cross-model hallucinations, to further decouple register from relevance.

\subsection{Architecture-Dependent Sensitivity}
\paragraph{Observation.} While the Llama-3.1-8B model exhibited a consistent performance boost under C3 for the BBBP task, the larger Llama-3.3-70B model showed a reversed effect, with hallucination augmentation degrading its strong baseline performance (0.644 to 0.549). On the BACE task, however, both model scales exhibited similar distributional compression and performance degradation under C3.

\paragraph{Interpretation.} This divergence may suggest that sensitivity to semantic perturbations is non-linearly related to model capacity and baseline domain knowledge. One possible interpretation is that smaller models, possessing lower baseline chemical reasoning capacity, may rely more heavily on semantically enriched perturbations as a form of "noisy intuition," whereas larger models, having captured more robust latent chemical representations during pre-training, may be more sensitive to the noise introduced by fabrications. Interestingly, the larger model's baseline on BACE (0.564) was lower than that of the 8B model (0.644), indicating that increased parameter scale does not uniformly translate to improved zero-shot performance across all chemical tasks. We offer these as tentative hypotheses, noting that further investigation into the interaction between model scale and perturbation sensitivity is required.

\subsection{Scope and Limitations}
To prevent overinterpretation, we explicitly bound our claims:
\begin{itemize}
    \item We report observational associations within a controlled perturbation framework; we do not establish a causal mechanism for how hallucinations alter LLM predictions.
    \item While we include cross-model validation, this was limited to two conditions (C0, C3) and a single inference run for the Llama-3.3-70B model, precluding confidence interval estimation for the larger architecture. The full nine-condition ablation across multiple seeds for the 70B model remains as future work.
    \item The observed utility of structurally grounded (SP) perturbations is strictly bounded to the BBBP dataset and the tested prompt templates. We do not claim this represents a generalized phenomenon across all molecular benchmarks.
    \item We do not propose hallucination augmentation as a practical, deployable method for molecular property prediction. The observed effects may be artifacts of the zero-shot evaluation setting rather than robust indicators of reasoning capacity.
    \item The proposed categorization scheme is operational, not exhaustive. The CC category is a residual class, and finer-grained semantic ontologies are required for comprehensive analysis.
    \item We note that traditional machine learning approaches, such as the Random Forest baseline reported in Section~\ref{sec:results}, are likely to achieve substantially higher predictive performance than the zero-shot LLM settings evaluated here.
\end{itemize}

Ultimately, these findings highlight that LLM-based molecular prediction pipelines can exhibit non-trivial sensitivity to semantically structured perturbations, suggesting that seemingly benign textual augmentations may introduce systematic biases in downstream predictions. Understanding this sensitivity is critical for interpreting LLM-assisted predictions, particularly in settings where textual inputs are used as auxiliary signals.
